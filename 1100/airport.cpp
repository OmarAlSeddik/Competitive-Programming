#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, m;
    cin >> n >> m;
    int a1[m], a2[m];
    for (int i = 0; i < m; i++) cin >> a1[i];
    sort(a1, a1+m);
    max 
    for (int i = 0; i < n; i++) {

    }
    
    cout << max << " " << min;
    return 0;
}